<%= form_for(@excuse) do |f| %>
  <% if @excuse.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@excuse.errors.count, "error") %> prohibited this excuse from being saved:</h2>

      <ul>
      <% @excuse.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
